# 소상공인 긴급 대타 매칭 서비스 실행팩

이 문서는 아래 방향을 실제 개발 단계로 넘기기 위한 **실행 문서**다.

- **서비스 본체:** 갑자기 빈 근무를, 검증된 경험자로 빠르게 메워주는 소상공인 긴급 대타 매칭 서비스
- **보조 전략:** 해결방안2(사장님이 다른 영업소에 알바로 일해주고 돈 받기)는 **공급 확보 장치 + 브랜딩 장치**로 활용
- **MVP 원칙:** 절대 넓게 시작하지 말고, **한 업종(카페) + 한 지역 + 당일/익일 긴급 공백**만 해결
- **핵심 판단:** 문제 강도는 강하지만, **신뢰/지역밀도/업종집중/재호출 구조**가 약하면 일반 구인앱처럼 무너짐

---

# 1. 랜딩페이지 실제 카피 버전

## 1-1. 페이지 목적
랜딩페이지의 목적은 아래 2가지만 달성하면 된다.

1. 사장님에게: “이건 구인앱이 아니라 **오늘 비는 근무를 해결하는 서비스**다”를 이해시키기
2. 근무자/사장님 대타에게: “빈 시간에 **검증된 대타**로 참여할 수 있다”를 이해시키기

불필요한 설명보다 **문제 강도 + 신뢰 + 즉시 행동**이 먼저 보여야 한다.

---

## 1-2. 랜딩페이지 정보 구조

1. 헤더
2. 히어로 섹션
3. 문제 공감 섹션
4. 해결 방식 섹션
5. 신뢰 장치 섹션
6. 재호출/반복사용 섹션
7. 공급자 유입 섹션
8. FAQ
9. 최종 CTA
10. 푸터

---

## 1-3. 헤더 카피

### 로고/서비스명
- 가칭: **급구커버**
- 대체 가칭: **오늘대타**, **커버점프**, **빈근무 해결**

### 메뉴
- 서비스 소개
- 사장님으로 시작하기
- 대타로 참여하기
- 로그인

### 우측 CTA 버튼
- **지금 공백 등록하기**

---

## 1-4. 히어로 섹션 실제 카피

### 메인 헤드라인
**갑자기 빈 근무를, 검증된 경험자로 빠르게 메우세요**

### 서브 헤드라인
카페 알바가 갑자기 취소했을 때,
당일·익일 공백을 근처의 경험자로 빠르게 연결해주는
**소상공인 긴급 대타 매칭 서비스**

### 신뢰 배지 문구
- 전화번호 인증
- 업종 경험 태그
- 출근률 공개
- 재호출 가능

### CTA 버튼
- **사장님으로 시작하기**
- **대타로 참여하기**

### 보조 문구
알바뿐 아니라, 빈 시간이 있는 사장님도 검증된 대타로 참여할 수 있습니다.

### 우측 예시 카드 UI 카피

#### 긴급 공고 카드
- 긴급
- 오늘 18:00 ~ 22:00
- 성수동 카페 / 마감 보조
- 시급 13,000원
- 경력자 우선
- 도보 12분
- **즉시 지원 가능**

#### 근무자 카드
- 김OO
- 카페 경력 2년
- 출근률 98%
- 최근 근무 12회
- 지금 출발 가능
- 지난 재호출 4회
- **바로 요청하기**

---

## 1-5. 문제 공감 섹션 카피

### 타이틀
**알바 공백 하나가, 하루 장사를 망칩니다**

### 본문
- 내일 오픈인데 사람이 비었습니다.
- 오늘 저녁 피크타임 4시간만 급합니다.
- 당근에 올려도, 알바앱에 올려도 지금 올 사람은 잘 안 잡힙니다.
- 결국 사장님이 직접 들어가고, 기존 직원이 버티고, 매장 컨디션이 무너집니다.

### 보조 카피
이 서비스는 느린 채용공고가 아니라,
**오늘 비는 근무를 해결하는 운영 복구 도구**입니다.

---

## 1-6. 해결 방식 섹션 카피

### 타이틀
**구인공고가 아니라, 운영 위기 복구 흐름으로 설계했습니다**

### 3단계 카드

#### 1) 급한 근무 등록
날짜, 시간, 시급, 필요한 경험만 입력하면
1분 안에 공백을 등록할 수 있습니다.

#### 2) 근처 경험자에게 즉시 알림
반경, 이동시간, 업종 경험 태그를 기준으로
지금 가능한 대타에게 바로 노출됩니다.

#### 3) 빠르게 확정하고 출근 체크인
확정 후에는 출근 전 확인과 체크인까지 이어져
“지원만 하고 안 오는 문제”를 줄입니다.

---

## 1-7. 신뢰 장치 섹션 카피

### 타이틀
**중요한 건 지원자 수가 아니라, 진짜 오는 사람입니다**

### 신뢰 포인트 카드

#### 전화번호 인증
최소한의 본인 확인이 끝난 사용자만 활동합니다.

#### 업종 경험 태그
카페 경력, 마감 가능, 포스 가능 등
바로 투입 가능한 정보를 먼저 봅니다.

#### 출근률 공개
잘 오는 사람과 불안한 사람을 구분할 수 있어야 합니다.

#### 거리/도착 시간 표시
긴급 근무는 실력만큼 거리와 이동시간이 중요합니다.

#### 체크인 기록
근무 시작 시점 체크인으로 실제 출근 여부를 남깁니다.

#### 재호출 가능
한 번 잘 맞았던 사람은 다음에도 다시 부를 수 있습니다.

---

## 1-8. 재호출 섹션 카피

### 타이틀
**한 번 잘 맞았던 사람은, 다음에도 다시 부를 수 있어야 합니다**

### 본문
매번 처음부터 찾지 마세요.
만족한 대타를 저장하고,
다음 공백 때 바로 다시 요청하세요.

### 포인트
- 즐겨찾기 저장
- 지난 근무 기록 확인
- 재호출 버튼 제공
- 재호출 많은 근무자 우선 신뢰

---

## 1-9. 공급자 유입 섹션 카피

### 타이틀
**추가 수입이 필요한 경험자라면, 빈 시간에 대타로 참여할 수 있습니다**

### 본문
카페 경력자, 주말·야간 추가근무 희망자,
그리고 빈 시간이 있는 사장님도
검증된 대타로 참여할 수 있습니다.

### 서브 문구
사장님도 때로는 사람을 구하는 입장이면서,
어떤 날에는 직접 일하러 갈 수 있는 공급자입니다.
이 현실을 공급망으로 연결합니다.

### CTA
- 대타로 등록하기

---

## 1-10. FAQ 카피

### Q1. 일반 알바앱이랑 뭐가 다른가요?
이 서비스는 장기 채용보다 **당일·익일 긴급 공백** 해결에 집중합니다.
경험, 거리, 출근률, 체크인 같은 **즉시 투입 신뢰 정보**를 우선 보여줍니다.

### Q2. 진짜 오는 사람인지 어떻게 믿나요?
전화번호 인증, 출근률, 취소/노쇼 기록, 체크인 기록을 기반으로 신뢰도를 제공합니다.

### Q3. 초보도 지원할 수 있나요?
공고마다 다릅니다. 사장님이 `경험자만`, `초보 가능` 여부를 직접 설정합니다.

### Q4. 사장님도 대타로 참여할 수 있나요?
가능합니다. 다만 메인 기능은 긴급 공백 해결이고, 사장님 대타는 공급 보완 장치로 운영합니다.

---

## 1-11. 최종 CTA 카피

### 타이틀
**오늘 비는 근무, 오늘 해결하세요**

### 설명
내일 오픈이 비었거나, 오늘 저녁 피크타임이 급하다면
지금 바로 공백을 등록하고 근처 경험자를 확인해보세요.

### 버튼
- **지금 공백 등록하기**
- **대타로 참여하기**

---

## 1-12. 푸터 카피
- 서비스 소개
- 이용약관
- 개인정보처리방침
- 문의하기
- 운영 지역 안내
- 현재 MVP: 카페 / 1개 지역 한정 운영

---

# 2. DB 스키마 버전 (MVP 기준)

## 2-1. 기술 전제
- 프론트엔드: Next.js App Router
- 백엔드/DB/Auth: Supabase
- 지도/위치: Kakao Maps 또는 Naver Maps 중 1개
- 알림: MVP는 앱 내 알림 + 문자/카카오 알림톡은 후순위
- 결제: MVP에서는 수기 결제 또는 가짜 결제 흐름으로 시작 가능. 실제 자동 결제는 2차

---

## 2-2. 설계 원칙
1. **역할을 너무 복잡하게 나누지 않는다**
2. `사장님`, `근무자`, `사장님 대타`는 하나의 users 테이블에서 role/flags로 표현한다
3. MVP는 **근무 확정과 체크인, 평가, 재호출**까지 돌아가게 하는 데 집중한다
4. 정산/세금/급여 자동화는 보류한다

---

## 2-3. 핵심 테이블 목록

1. users
2. owner_profiles
3. worker_profiles
4. stores
5. shift_requests
6. shift_applications
7. shift_matches
8. checkins
9. reviews
10. favorite_workers
11. notifications
12. reports

---

## 2-4. 테이블 상세 정의

## users
공통 사용자 기본 정보

```sql
create table users (
  id uuid primary key default gen_random_uuid(),
  auth_user_id uuid unique,
  role text not null check (role in ('owner', 'worker', 'owner_worker', 'admin')),
  name text not null,
  phone text,
  is_phone_verified boolean not null default false,
  profile_image_url text,
  status text not null default 'active' check (status in ('active', 'suspended', 'deleted')),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);
```

### 비고
- `auth_user_id`는 Supabase Auth 유저와 연결
- `owner_worker`는 사장님이면서 대타 근무도 가능한 사용자

---

## owner_profiles
사장님/매장 운영자 정보

```sql
create table owner_profiles (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references users(id) on delete cascade,
  business_type text,
  operating_years integer,
  intro text,
  region_text text,
  is_verified_owner boolean not null default false,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  unique(user_id)
);
```

### 비고
- MVP에서는 사업자등록증 인증은 수동 검토로 시작 가능

---

## worker_profiles
근무자 정보

```sql
create table worker_profiles (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references users(id) on delete cascade,
  is_owner_worker boolean not null default false,
  years_of_experience integer not null default 0,
  experience_tags text[] not null default '{}',
  availability_tags text[] not null default '{}',
  preferred_regions text[] not null default '{}',
  radius_km integer not null default 3,
  can_start_immediately boolean not null default false,
  attendance_score numeric(5,2) not null default 100,
  no_show_count integer not null default 0,
  late_count integer not null default 0,
  cancellation_count integer not null default 0,
  rehire_count integer not null default 0,
  intro text,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  unique(user_id)
);
```

### experience_tags 예시
- `카페경력`
- `포스가능`
- `오픈가능`
- `마감가능`
- `홀응대`
- `음료제조`
- `주말가능`
- `야간가능`

---

## stores
매장 정보

```sql
create table stores (
  id uuid primary key default gen_random_uuid(),
  owner_user_id uuid not null references users(id) on delete cascade,
  store_name text not null,
  category text not null default 'cafe',
  address text not null,
  detail_address text,
  lat numeric(10,7),
  lng numeric(10,7),
  phone text,
  description text,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);
```

### 비고
- MVP는 `category = cafe` 고정 가능

---

## shift_requests
사장님이 등록한 긴급 공백

```sql
create table shift_requests (
  id uuid primary key default gen_random_uuid(),
  store_id uuid not null references stores(id) on delete cascade,
  owner_user_id uuid not null references users(id) on delete cascade,
  shift_date date not null,
  start_time time not null,
  end_time time not null,
  hourly_wage integer not null,
  urgency_level text not null default 'high' check (urgency_level in ('normal', 'high', 'urgent')),
  experience_required boolean not null default true,
  allow_beginner boolean not null default false,
  task_tags text[] not null default '{}',
  memo text,
  worker_type_preference text not null default 'any' check (worker_type_preference in ('any', 'worker_only', 'owner_worker_preferred')),
  status text not null default 'open' check (status in ('open', 'matched', 'completed', 'cancelled')),
  deadline_at timestamptz,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);
```

### task_tags 예시
- `오픈`
- `마감`
- `포스`
- `음료`
- `청소`
- `홀응대`

---

## shift_applications
근무자 지원 내역

```sql
create table shift_applications (
  id uuid primary key default gen_random_uuid(),
  shift_request_id uuid not null references shift_requests(id) on delete cascade,
  worker_user_id uuid not null references users(id) on delete cascade,
  status text not null default 'applied' check (status in ('applied', 'withdrawn', 'accepted', 'rejected', 'cancelled')),
  applied_at timestamptz not null default now(),
  unique(shift_request_id, worker_user_id)
);
```

---

## shift_matches
최종 확정된 매칭

```sql
create table shift_matches (
  id uuid primary key default gen_random_uuid(),
  shift_request_id uuid not null references shift_requests(id) on delete cascade,
  worker_user_id uuid not null references users(id) on delete cascade,
  application_id uuid references shift_applications(id) on delete set null,
  match_status text not null default 'confirmed' check (match_status in ('confirmed', 'checked_in', 'completed', 'cancelled', 'no_show')),
  confirmed_at timestamptz not null default now(),
  checked_in_at timestamptz,
  completed_at timestamptz,
  cancelled_at timestamptz,
  no_show_reported_at timestamptz,
  unique(shift_request_id)
);
```

### 비고
- 하나의 긴급 공백당 최종 매칭은 1명

---

## checkins
실제 출근 체크인 기록

```sql
create table checkins (
  id uuid primary key default gen_random_uuid(),
  shift_match_id uuid not null references shift_matches(id) on delete cascade,
  worker_user_id uuid not null references users(id) on delete cascade,
  checked_in_at timestamptz not null default now(),
  lat numeric(10,7),
  lng numeric(10,7),
  note text
);
```

---

## reviews
근무 후 상호 평가

```sql
create table reviews (
  id uuid primary key default gen_random_uuid(),
  shift_match_id uuid not null references shift_matches(id) on delete cascade,
  reviewer_user_id uuid not null references users(id) on delete cascade,
  target_user_id uuid not null references users(id) on delete cascade,
  rating integer not null check (rating between 1 and 5),
  review_type text not null check (review_type in ('owner_to_worker', 'worker_to_owner')),
  comment text,
  created_at timestamptz not null default now()
);
```

---

## favorite_workers
재호출용 즐겨찾기

```sql
create table favorite_workers (
  id uuid primary key default gen_random_uuid(),
  owner_user_id uuid not null references users(id) on delete cascade,
  worker_user_id uuid not null references users(id) on delete cascade,
  created_at timestamptz not null default now(),
  unique(owner_user_id, worker_user_id)
);
```

---

## notifications
앱 내 알림

```sql
create table notifications (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references users(id) on delete cascade,
  type text not null,
  title text not null,
  body text,
  related_id uuid,
  is_read boolean not null default false,
  created_at timestamptz not null default now()
);
```

### notification type 예시
- `new_shift_nearby`
- `application_received`
- `match_confirmed`
- `checkin_reminder`
- `review_request`
- `rehire_request`

---

## reports
신고/패널티 처리용

```sql
create table reports (
  id uuid primary key default gen_random_uuid(),
  reporter_user_id uuid not null references users(id) on delete cascade,
  target_user_id uuid not null references users(id) on delete cascade,
  shift_match_id uuid references shift_matches(id) on delete set null,
  report_type text not null,
  detail text,
  status text not null default 'open' check (status in ('open', 'reviewing', 'resolved', 'rejected')),
  created_at timestamptz not null default now(),
  resolved_at timestamptz
);
```

---

## 2-5. 권장 인덱스

```sql
create index idx_shift_requests_status_date on shift_requests(status, shift_date);
create index idx_shift_requests_store_id on shift_requests(store_id);
create index idx_shift_applications_shift_request_id on shift_applications(shift_request_id);
create index idx_shift_matches_worker_user_id on shift_matches(worker_user_id);
create index idx_notifications_user_id_is_read on notifications(user_id, is_read);
```

---

## 2-6. MVP 상태 값 흐름

### shift_requests.status
- `open` → 모집 중
- `matched` → 최종 확정됨
- `completed` → 근무 완료
- `cancelled` → 공백 취소

### shift_matches.match_status
- `confirmed` → 확정
- `checked_in` → 출근 체크인 완료
- `completed` → 완료
- `cancelled` → 확정 후 취소
- `no_show` → 무단 불참

---

## 2-7. 추천 RLS 방향

### 사장님
- 자기 store, 자기 shift_request, 자기 지원자 목록만 조회 가능

### 근무자
- open 상태의 공개 공고 조회 가능
- 자기 지원내역과 자기 프로필만 수정 가능

### 운영자(admin)
- 전체 조회/수정 가능

---

# 3. 화면 와이어프레임 버전

아래는 디자이너 없이도 Codex가 바로 구현 가능한 **텍스트 와이어프레임**이다.

---

## 3-1. 공통 레이아웃

### 상단 헤더
- 좌측: 로고
- 중앙/우측: 메뉴
- 우측 버튼: `공백 등록하기`

### 모바일 하단 탭
- 홈
- 긴급공고
- 알림
- 채팅
- 마이

---

## 3-2. 랜딩페이지 와이어프레임

```text
[헤더]
로고 | 서비스 소개 | 사장님으로 시작하기 | 대타로 참여하기 | 로그인 | [지금 공백 등록하기]

[히어로]
H1: 갑자기 빈 근무를, 검증된 경험자로 빠르게 메우세요
P: 카페 알바가 갑자기 취소했을 때...
[사장님으로 시작하기] [대타로 참여하기]
배지: 전화번호 인증 / 업종 경험 태그 / 출근률 공개 / 재호출 가능
오른쪽 미리보기 카드 2개

[문제 공감]
타이틀 + 4개 문장

[해결 방식]
카드 3개
1. 1분 등록
2. 근처 경험자 즉시 알림
3. 출근 체크인까지 확인

[신뢰 섹션]
카드 6개

[재호출 섹션]
저장 / 재호출 / 기록

[공급자 참여 섹션]
대타 등록 설명 + CTA

[FAQ]
4개

[최종 CTA]
오늘 비는 근무, 오늘 해결하세요
[지금 공백 등록하기] [대타로 참여하기]

[푸터]
```

---

## 3-3. 사장님 대시보드 와이어프레임

```text
[상단]
안녕하세요, 김사장님
오늘 공백 1건 | 진행 중 매칭 2건 | 재호출 가능 인력 4명

[빠른 액션]
[+ 긴급 공백 등록]

[현재 진행 중 공백 카드 리스트]
- 오늘 18:00~22:00 / 성수 카페 / 마감 / 긴급 / 지원 3명 / [지원자 보기]
- 내일 07:00~11:00 / 오픈 / 매칭 완료 / [상세 보기]

[추천 대타]
- 출근률 높은 순
- 가까운 순
- 지난번 근무자 우선

[재호출 섹션]
지난번 만족한 근무자 카드 목록 + [다시 요청]
```

---

## 3-4. 공백 등록 페이지 와이어프레임

```text
[타이틀] 긴급 공백 등록

업종: 카페 (고정)
근무 날짜: [date picker]
시작 시간: [time]
종료 시간: [time]
시급: [input]
업무 태그: [오픈] [마감] [포스] [음료] [홀응대]
경험자만 받기: [toggle]
초보 가능: [toggle]
긴급도: [normal/high/urgent]
추가 메모: [textarea]
선호 근무자: [상관없음 / 사장님 대타 우선]

[등록 버튼] 지금 공백 등록하기
```

### UX 포인트
- 한 화면에서 끝나게 한다
- 입력 항목은 최소화하되, 즉시 투입 판단에 필요한 정보는 꼭 받는다

---

## 3-5. 지원자 목록 페이지 와이어프레임

```text
[타이틀] 지원자 5명

[필터]
출근률 높은 순 | 가까운 순 | 재호출 많은 순

[지원자 카드]
- 이름/닉네임
- 카페 경력 2년
- 출근률 98%
- 거리 1.2km / 도착 11분
- 태그: 마감 가능, 포스 가능
- 최근 근무 12회
- 사장님 대타 배지 (있을 때)
- [프로필 보기] [바로 확정]
```

---

## 3-6. 근무자 홈(긴급공고 피드) 와이어프레임

```text
[상단]
지금 출발 가능 [toggle]
반경 3km [dropdown]
오늘/내일 [tab]

[긴급 공고 카드 리스트]
- 긴급 배지
- 오늘 18:00~22:00
- 성수동 카페
- 시급 13,000원
- 태그: 마감 / 포스
- 도보 12분
- 경험자 우선
- [지원하기]
```

### UX 포인트
- 일반 채용공고처럼 길게 읽지 않게 한다
- 카드 정보만 보고도 지원 여부를 빠르게 결정하게 만든다

---

## 3-7. 근무자 프로필 페이지 와이어프레임

```text
[프로필 상단]
프로필 사진
이름
출근률 98%
최근 근무 12회
재호출 4회

[기본 정보]
카페 경력 2년
가능 태그: 오픈, 마감, 포스
가능 시간: 평일 저녁 / 주말 종일
반경: 3km
지금 출발 가능: ON

[사장님 대타 정보]
현재 운영 업종: 개인 카페
운영 연차: 3년
배지: 사장님 대타 인증

[수정 버튼]
```

---

## 3-8. 매칭 상세 / 체크인 페이지 와이어프레임

```text
[상단]
오늘 18:00~22:00 / 성수동 카페 / 마감

[상태]
확정됨
근무 시작까지 01:24:12

[사장님 연락처]
[전화] [채팅]

[근무 안내]
주소
준비물
추가 메모

[체크인]
근무 시작 후 [출근 체크인]
```

---

## 3-9. 재호출 페이지 와이어프레임

```text
[타이틀] 지난번 만족한 대타

[근무자 카드 리스트]
- 이름
- 최근 함께 일한 날짜
- 평점 4.8
- 재호출 3회
- 거리
- [다시 요청]
```

---

## 3-10. 운영자(Admin) 화면 와이어프레임

```text
[대시보드]
오늘 신규 공고 수
오늘 매칭 수
오늘 체크인 수
신고 접수 수

[사용자 관리]
사장님 / 근무자 / 사장님 대타 구분
상태 변경 active/suspended

[신고 관리]
노쇼 신고 상세
패널티 수동 처리

[매칭 관리]
오픈 / 매칭 / 완료 / 취소 현황
```

---

# 4. Codex에게 바로 보낼 개발 요청문

아래는 그대로 복사해서 붙여넣을 수 있는 개발 프롬프트다.

---

## 4-1. Codex용 전체 프롬프트

```md
다음 명세에 맞춰 MVP 웹서비스를 구현해줘.

# 프로젝트명
소상공인 긴급 대타 매칭 서비스 MVP

# 서비스 한 줄 정의
갑자기 빈 근무를, 검증된 경험자로 빠르게 메워주는 소상공인 긴급 대타 매칭 서비스

# 핵심 방향
- 이 서비스는 일반 구인구직 플랫폼이 아니라 운영 위기 복구 서비스처럼 보여야 한다.
- 해결방안2(사장님이 다른 영업소에 알바로 일해주고 돈 받기)는 본체가 아니라 공급 확보/브랜딩 장치로 넣는다.
- MVP는 절대 넓게 시작하지 말고 카페 1개 업종, 1개 지역, 당일/익일 긴급 공백만 다룬다.

# 기술 스택
- Next.js (App Router)
- TypeScript
- Tailwind CSS
- Supabase (Auth + Database)
- 필요시 shadcn/ui 사용

# 구현 목표
아래 핵심 흐름이 실제로 동작해야 한다:
1. 사장님 회원가입/로그인
2. 매장 등록
3. 긴급 공백 등록
4. 근무자 회원가입/프로필 등록
5. 근무자 공고 탐색 및 지원
6. 사장님이 지원자 확인 후 1명 확정
7. 근무자가 체크인
8. 근무 완료 후 상호 평가
9. 사장님이 이전 근무자를 즐겨찾기하고 재호출 가능

# 주요 화면
1. 랜딩페이지
2. 로그인/회원가입
3. 사장님 대시보드
4. 공백 등록 페이지
5. 지원자 목록 페이지
6. 근무자 긴급공고 피드
7. 근무자 프로필 페이지
8. 매칭 상세/체크인 페이지
9. 재호출 페이지
10. 관리자 페이지(최소형)

# 랜딩페이지 카피 방향
- 헤드라인: 갑자기 빈 근무를, 검증된 경험자로 빠르게 메우세요
- 서브카피: 카페 알바가 갑자기 취소했을 때, 당일·익일 공백을 근처의 경험자로 빠르게 연결해주는 소상공인 긴급 대타 매칭 서비스
- CTA 버튼 2개: 사장님으로 시작하기 / 대타로 참여하기
- 신뢰 포인트: 전화번호 인증, 업종 경험 태그, 출근률 공개, 재호출 가능
- 공급자 유입 문구: 알바뿐 아니라 빈 시간이 있는 사장님도 검증된 대타로 참여할 수 있음

# 데이터 모델
아래 테이블을 Supabase 기준으로 구현해줘.
- users
- owner_profiles
- worker_profiles
- stores
- shift_requests
- shift_applications
- shift_matches
- checkins
- reviews
- favorite_workers
- notifications
- reports

각 테이블은 이 명세를 따라 migration SQL도 같이 작성해줘.

## users
- id (uuid, PK)
- auth_user_id (uuid, unique)
- role ('owner', 'worker', 'owner_worker', 'admin')
- name
- phone
- is_phone_verified
- profile_image_url
- status ('active', 'suspended', 'deleted')
- created_at
- updated_at

## owner_profiles
- id
- user_id
- business_type
- operating_years
- intro
- region_text
- is_verified_owner
- created_at
- updated_at

## worker_profiles
- id
- user_id
- is_owner_worker
- years_of_experience
- experience_tags (text[])
- availability_tags (text[])
- preferred_regions (text[])
- radius_km
- can_start_immediately
- attendance_score
- no_show_count
- late_count
- cancellation_count
- rehire_count
- intro
- created_at
- updated_at

## stores
- id
- owner_user_id
- store_name
- category (기본값 cafe)
- address
- detail_address
- lat
- lng
- phone
- description
- created_at
- updated_at

## shift_requests
- id
- store_id
- owner_user_id
- shift_date
- start_time
- end_time
- hourly_wage
- urgency_level ('normal', 'high', 'urgent')
- experience_required
- allow_beginner
- task_tags (text[])
- memo
- worker_type_preference ('any', 'worker_only', 'owner_worker_preferred')
- status ('open', 'matched', 'completed', 'cancelled')
- deadline_at
- created_at
- updated_at

## shift_applications
- id
- shift_request_id
- worker_user_id
- status ('applied', 'withdrawn', 'accepted', 'rejected', 'cancelled')
- applied_at
- unique(shift_request_id, worker_user_id)

## shift_matches
- id
- shift_request_id
- worker_user_id
- application_id
- match_status ('confirmed', 'checked_in', 'completed', 'cancelled', 'no_show')
- confirmed_at
- checked_in_at
- completed_at
- cancelled_at
- no_show_reported_at
- unique(shift_request_id)

## checkins
- id
- shift_match_id
- worker_user_id
- checked_in_at
- lat
- lng
- note

## reviews
- id
- shift_match_id
- reviewer_user_id
- target_user_id
- rating (1~5)
- review_type ('owner_to_worker', 'worker_to_owner')
- comment
- created_at

## favorite_workers
- id
- owner_user_id
- worker_user_id
- created_at
- unique(owner_user_id, worker_user_id)

## notifications
- id
- user_id
- type
- title
- body
- related_id
- is_read
- created_at

## reports
- id
- reporter_user_id
- target_user_id
- shift_match_id
- report_type
- detail
- status ('open', 'reviewing', 'resolved', 'rejected')
- created_at
- resolved_at

# 핵심 기능 요구사항

## 1. 긴급 공백 등록
- 사장님은 날짜, 시간, 시급, 업무 태그, 초보 가능 여부, 긴급도, 메모를 입력해 공백을 등록할 수 있어야 함
- UX는 1분 안에 끝날 정도로 단순해야 함

## 2. 근무자 프로필/신뢰도
- 근무자 프로필에 출근률, 경력, 태그, 반경, 지금 출발 가능 여부가 보여야 함
- 사장님 대타는 별도 배지로 표시

## 3. 긴급공고 피드
- 근무자는 open 상태 공고를 리스트로 보고 지원할 수 있어야 함
- 정렬 기준: 긴급순, 가까운 순, 최신순

## 4. 지원자 확인 및 확정
- 사장님은 지원자 리스트를 보고 1명을 확정 가능해야 함
- 확정 시 shift_requests.status는 matched, shift_matches 레코드 생성

## 5. 출근 체크인
- 확정된 근무자는 매칭 상세 화면에서 체크인할 수 있어야 함
- 체크인 시 shift_matches.match_status는 checked_in으로 변경

## 6. 근무 완료 및 평가
- 사장님과 근무자는 서로 리뷰를 남길 수 있어야 함
- 추후 attendance_score 계산용 기반으로 남겨둘 것

## 7. 재호출
- 사장님은 근무 완료한 근무자를 favorite_workers에 저장하고 다시 요청할 수 있어야 함

# UI/UX 요구사항
- 모바일 우선 반응형
- 일반 채용공고 사이트처럼 길고 무겁지 않게
- 긴급성 강조 배지, 카운트다운 느낌, 간단한 카드 중심 UI
- 신뢰 정보(출근률, 거리, 경험 태그)를 카드 상단에서 바로 보이게 할 것

# 관리자 기능
- 최소형 관리자 페이지 필요
- 사용자 상태 변경(active/suspended)
- 신고 목록 조회
- 공고/매칭 현황 조회

# 구현 우선순위
1. Auth + DB 스키마 + 시드데이터
2. 랜딩페이지
3. 사장님 플로우
4. 근무자 플로우
5. 매칭/체크인/리뷰
6. 재호출
7. 최소 관리자

# 결과물 요청
- 전체 프로젝트 코드
- README
- Supabase migration SQL
- 환경변수 예시 파일
- 주요 페이지/컴포넌트 설명
- 로컬 실행 방법
```

---

# 5. 개발 우선순위 체크리스트

## Phase 1. 뼈대
- [ ] Next.js 프로젝트 세팅
- [ ] Tailwind 세팅
- [ ] Supabase 연동
- [ ] Auth 기본 로그인/회원가입
- [ ] DB migration 작성

## Phase 2. 랜딩페이지
- [ ] 히어로 섹션
- [ ] 문제 공감 섹션
- [ ] 해결 방식 섹션
- [ ] 신뢰 섹션
- [ ] 공급자 유입 섹션
- [ ] CTA 섹션

## Phase 3. 사장님 기능
- [ ] 매장 등록
- [ ] 공백 등록
- [ ] 공백 목록
- [ ] 지원자 목록
- [ ] 1명 확정

## Phase 4. 근무자 기능
- [ ] 프로필 등록
- [ ] 공고 리스트
- [ ] 공고 상세
- [ ] 지원
- [ ] 체크인

## Phase 5. 반복 사용 기능
- [ ] 리뷰
- [ ] 즐겨찾기
- [ ] 재호출

## Phase 6. 운영 기능
- [ ] 신고 관리
- [ ] 사용자 상태 관리
- [ ] 기본 통계

---

# 6. 가장 중요한 실행 메모

1. 처음부터 전국 서비스처럼 보이면 안 된다.
2. MVP에서는 카페 1개 업종만 보여야 한다.
3. 랜딩페이지의 언어는 “채용”보다 “오늘 비는 근무 해결” 중심이어야 한다.
4. 신뢰 정보가 공고보다 먼저 눈에 띄어야 한다.
5. 해결방안2는 메인이 아니라 공급망 보완용이다.
6. 성공 기준은 회원 수가 아니라 **실제 매칭 → 체크인 → 재호출**이다.

